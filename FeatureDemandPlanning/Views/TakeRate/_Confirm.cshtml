@model FeatureDemandPlanning.Model.ViewModel.TakeRateDataViewModel

<div class="jumbotron">
    
        <p>The following import data files have been selected:</p>

        <ul>
            @foreach (var import in Model.TakeRate.FdpTakeRateHeaders)
            {
                <li>@import</li>
            }
        </ul>

        <p>This take rate data will be applied to the OXO document:</p>

        <ul>
            <li>@Model.TakeRate.Document.Name</li>
        </ul>
        
        <p>Click 'Next' to continue</p>
</div>
