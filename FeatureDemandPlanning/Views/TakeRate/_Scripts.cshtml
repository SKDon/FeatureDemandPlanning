@model FeatureDemandPlanning.Model.ViewModel.TakeRateViewModel

<script type="text/javascript">

    var takeRates, page, modal;
    var params = {
        ActionsUri: "@Url.RouteUrl("TakeRateMenu")",
        TakeRateUri: "@Url.RouteUrl("TakeRate")",
        TakeRatesUri: "@Url.RouteUrl("ListTakeRates")",
        ModalContentUri: "@Url.RouteUrl("TakeRateContent")",
        ModalActionUri: "@Url.RouteUrl("TakeRateAction")",
        ModalDialogId: "modal",
        ModalContentId: "modalBody",
        Configuration: {},
        PageSize: @Model.PageSize,
        PageIndex: @Model.PageIndex,
        TakeRateId: @Model.TakeRate.TakeRateId,
        IdentifierPrefix: "@Model.IdentifierPrefix",
        _lastParam: null // Prevent syntax error
    };

    params.Configuration = @Html.Raw(Json.Encode(Model.Configuration));
</script>

@Scripts.Render("~/bundles/js/shared")
@Scripts.Render("~/bundles/js/takerate")
<script language="javascript" src="@Url.Content("~/Content/js/TakeRate/init.js")?v=@Model.CurrentVersion"></script>